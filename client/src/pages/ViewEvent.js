// on click of reserve button, if user is not logged in, alert they must be logged in, redirect to log in or trigger a login modal

// if logged in, on click of reserve button, pop up modal to get users, first and last name, email, num of people, age verification if needed
// email verification to check if this email has already made a reservation
// if they have, alert 'this email is already on the reservation list'

import React from 'react'

const ViewEvent = () => {
  return (
    <div>ViewEvent</div>
  )
}

export default ViewEvent